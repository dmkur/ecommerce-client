const baseURL = 'http://localhost:5000/api'
const urls = {
    users: '/users',
    products: '/products',
    orders:"/orders",
    payment:"/checkout/payment",
    auth:"/auth/login",
}

export {baseURL, urls}
